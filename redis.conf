# Redis 配置文件 - 用于 RBAC Demo

# 数据持久化
appendonly yes
appendfilename "appendonly.aof"
appendfsync everysec

# 保存策略（RDB）
save 900 1
save 300 10
save 60 10000

# 最大内存限制（256MB）
maxmemory 256mb

# 内存满时，拒绝写入
maxmemory-policy noeviction

# 网络配置
bind 0.0.0.0
protected-mode no
port 6379
# 🔥 设置为 0 表示不关闭空闲连接，依赖客户端的 keepalive
timeout 0
# 每60秒发送 TCP keepalive 探测包
tcp-keepalive 60

# 日志
loglevel notice
logfile ""

# 数据库数量
databases 16

# 性能优化
hz 1
dynamic-hz yes
