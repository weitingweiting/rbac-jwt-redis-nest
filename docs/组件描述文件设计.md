## 5. 组件描述文件设计

### 5.1 component.meta.json 完整结构

```json
{
  "id": "BarChart",
  "name": "柱状图",
  "version": "1.0.0",
  "description": "基于 ECharts 的通用柱状图组件，支持多种数据展示方式",
  "classification": {
    "level1": "chart",
    "level2": "bar",
    "displayName": {
      "level1": "图表",
      "level2": "柱状图"
    }
  },
  "type": "vue-echarts",
  "framework": "vue3",
  "author": {
    "organization": "江苏电力公司",
    "userName": "weiting"
  },
  "files": {
    "entry": "index.umd.js",
    "style": "style.css",
    "preview": "assets/preview.png"
  },
  "buildInfo": {
    "buildTime": "2025-12-20T14:30:00.000Z",
    "hash": "abc123def456",
    "cliVersion": "1.0.0"
  },
  "license": "MIT"
}
```

### 5.2 字段说明

| 字段                         | 类型   | 必需 | 说明                                           |
| ---------------------------- | ------ | ---- | ---------------------------------------------- |
| `id`                         | string | ✅   | 组件唯一标识（英文，PascalCase，如：BarChart） |
| `name`                       | string | ✅   | 组件中文名称（如：柱状图）                     |
| `version`                    | string | ✅   | 语义化版本号（如：1.0.0）                      |
| `description`                | string | ❌   | 组件功能描述                                   |
| `classification`             | object | ✅   | **组件分类信息**（用于管理后台分类展示）       |
| `classification.level1`      | string | ✅   | 一级分类标识（英文：chart/table/form）         |
| `classification.level2`      | string | ✅   | 二级分类标识（英文：bar/line/pie）             |
| `classification.displayName` | object | ✅   | 分类展示名称（中文）                           |
| `type`                       | string | ✅   | 组件模板类型（vue-echarts/vue-component/js）   |
| `framework`                  | string | ✅   | 开发框架（vue3/js）                            |
| `author`                     | object | ❌   | 作者信息                                       |
| `files`                      | object | ✅   | **打包产物文件清单**（记录打包后生成的文件名） |
| `files.entry`                | string | ✅   | 主入口文件（index.umd.js）                     |
| `files.style`                | string | ✅   | 样式文件（style.css）                          |
| `files.preview`              | string | ✅   | 预览图（preview.png）                          |
| `buildInfo`                  | object | ✅   | 构建信息（自动生成）                           |
| `buildInfo.buildTime`        | string | ✅   | 构建时间（ISO 格式）                           |
| `buildInfo.hash`             | string | ✅   | 文件哈希值（用于缓存控制）                     |
| `buildInfo.cliVersion`       | string | ✅   | CLI 版本号                                     |
| `license`                    | string | ❌   | 许可证（如：MIT）                              |

### 5.3 关键字段详解

#### 1. `classification` - 组件分类

**作用：** 描述组件的分类归属，用于管理后台构建分类树结构。

```javascript
// 示例：柱状图组件
"classification": {
  "level1": "chart",        // 一级分类：图表
  "level2": "bar",          // 二级分类：柱状图
  "displayName": {
    "level1": "图表",       // 一级显示名
    "level2": "柱状图"      // 二级显示名
  }
}

// 管理后台展示效果：
// 📊 图表
//   ├─ 📊 柱状图
//   │   └─ BarChart (柱状图)
//   ├─ 📈 折线图
//   └─ 🥧 饼图
```

### 最新的打包目录结构示例

```dist/
├── assets/
│   └── preview.png
├── component.meta.json
├── index.esm.js
├── index.esm.js.map
├── style.css
```
