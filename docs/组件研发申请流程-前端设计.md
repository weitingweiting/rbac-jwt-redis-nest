# 组件研发申请流程 - 前端设计方案

## 📋 目录

- [1. 概述](#1-概述)
- [2. 页面架构](#2-页面架构)
- [3. 页面详细设计](#3-页面详细设计)
- [4. 状态与交互设计](#4-状态与交互设计)
- [5. API 接口与 DTO](#5-api-接口与-dto)

---

## 1. 概述

### 1.1 设计目标

基于 **"先审批，后开发"** 的核心理念，设计简洁高效的前端交互流程。

| 设计原则          | 说明                              |
| ----------------- | --------------------------------- |
| 🎯 **流程可视化** | 使用 `n-steps` 清晰展示申请进度   |
| 📊 **信息结构化** | 使用 `n-data-table` 管理申请列表  |
| 🌳 **分类层级化** | 使用 `n-tree-select` 选择组件分类 |
| 🔔 **状态醒目化** | 使用 `n-tag` 区分不同状态         |
| 🎭 **抽屉详情**   | 使用 `n-drawer` 展示申请详情      |

### 1.2 技术栈

- **UI 框架**：Naive UI
- **前端框架**：Vue 3 + TypeScript
- **状态管理**：Pinia
- **HTTP 客户端**：Axios

### 1.3 核心状态（5 个）

| 状态             | 标签文本   | Tag 类型  | 说明                        |
| ---------------- | ---------- | --------- | --------------------------- |
| `pending_review` | 待审核     | `warning` | 等待管理员审批              |
| `approved`       | 审核通过   | `success` | 可下载 supplement，开始开发 |
| `rejected`       | 审核不通过 | `error`   | 终态                        |
| `completed`      | 已完成     | `success` | 组件已入库                  |
| `cancelled`      | 已取消     | `default` | 终态                        |

---

## 2. 页面架构

### 2.1 页面结构

```
组件研发申请模块
├── 申请管理（普通用户）
│   ├── 我的申请列表页 ........... /development/applications
│   │   └── 申请详情抽屉 ......... n-drawer 组件
│   └── 创建申请页 .............. /development/applications/create
│
└── 审核管理（管理员）
    └── 待审核列表页 ............ /development/reviews
```

### 2.2 路由配置

| 路由                               | 页面名称     | 权限     | 说明                    |
| ---------------------------------- | ------------ | -------- | ----------------------- |
| `/development/applications`        | 我的申请列表 | `read`   | 查看个人申请 + 详情抽屉 |
| `/development/applications/create` | 创建申请     | `create` | 发起新申请              |
| `/development/reviews`             | 待审核列表   | `review` | 管理员审核入口          |

### 2.3 页面功能矩阵

| 页面             | 主要功能                          | 核心组件                                              |
| ---------------- | --------------------------------- | ----------------------------------------------------- |
| **我的申请列表** | 筛选、搜索、查看、取消 + 详情抽屉 | `n-data-table`, `n-tag`, `n-select`, `n-drawer`       |
| **申请详情抽屉** | 查看进度、下载文件、上传组件      | `n-steps`, `n-descriptions`, `n-upload`, `n-timeline` |
| **创建申请**     | 选择类型、填写信息、选择分类      | `n-steps`, `n-form`, `n-tree-select`, `n-card`        |
| **待审核列表**   | 筛选、批量审核、快速操作          | `n-data-table`, `n-modal`, `n-tag`                    |

---

## 3. 页面详细设计

### 3.1 我的申请列表页

#### 页面布局

```
┌─────────────────────────────────────────────────────────────────┐
│  组件研发申请                                    [+ 创建申请]   │
├─────────────────────────────────────────────────────────────────┤
│  筛选区域                                                       │
│  ┌──────────┐ ┌──────────┐ ┌────────────────┐ ┌──────┐         │
│  │ 状态筛选 │ │ 类型筛选 │ │ 搜索组件名/ID  │ │ 搜索 │         │
│  └──────────┘ └──────────┘ └────────────────┘ └──────┘         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ 申请单号      │ 类型   │ 组件ID    │ 版本  │ 状态   │ 操作  ││
│  ├─────────────────────────────────────────────────────────────┤│
│  │ APP-20260107  │ 新组件 │ DataTable │ 1.0.0 │ 待审核 │ ...   ││
│  │ APP-20260106  │ 迭代   │ BarChart  │ 2.1.0 │ 通过   │ ...   ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                 │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │                    < 1 2 3 ... 10 >                         ││
│  └─────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────┘
```

#### 功能说明

| 功能         | 交互说明                                              |
| ------------ | ----------------------------------------------------- |
| **状态筛选** | `n-select` 下拉选择，支持多选                         |
| **类型筛选** | `n-select` 下拉：新组件/版本迭代/替换版本             |
| **搜索**     | `n-input` 支持组件 ID 或名称模糊搜索                  |
| **查看详情** | 点击申请单号打开右侧抽屉                              |
| **取消申请** | 仅 `pending_review` 状态显示，`n-popconfirm` 二次确认 |

#### 表格列定义

| 列名     | 字段                | 宽度  | 说明             |
| -------- | ------------------- | ----- | ---------------- |
| 申请单号 | `applicationNo`     | 180px | 可点击打开抽屉   |
| 申请类型 | `applicationType`   | 100px | `n-tag` 展示     |
| 组件 ID  | `componentId`       | 150px | -                |
| 组件名称 | `name`              | 150px | 仅新组件显示     |
| 目标版本 | `targetVersion`     | 100px | -                |
| 状态     | `developmentStatus` | 120px | `n-tag` 不同颜色 |
| 创建时间 | `createdAt`         | 180px | 格式化显示       |
| 操作     | -                   | 150px | 查看、取消       |

#### 详情抽屉（n-drawer）

点击申请单号时，从右侧弹出抽屉展示详情：

```
┌────────────────────────────┐
│  列表页面                 │  ┌───────────────────────────────────┐
│                            │  │  申请详情 - APP-20260107-0001  ✕  │
│  申请列表...              │  ├───────────────────────────────────┤
│                            │  │                                   │
│  [点击申请单号]           │  │  ┌─────────────────────────────┐ │
│   └─> 打开抽屉            │  │  │  ① 提交 ── ② 审核 ── ③ 上传 ── ④ 完成│ │
│                            │  │  │   ✓      ●       ○       ○  │ │
│                            │  │  └─────────────────────────────┘ │
│                            │  │                                   │
│                            │  │  📋 申请信息                     │
│                            │  │  ──────────────────────────────  │
│                            │  │  申请单号: APP-20260107-0001     │
│                            │  │  组件ID: DataTable               │
│                            │  │  ...                             │
│                            │  │                                   │
│                            │  │  📝 操作区域                     │
│                            │  │  ┌─────────────────────────────┐ │
│                            │  │  │  [下载 supplement.json]   │ │
│                            │  │  │  [上传组件包]             │ │
│                            │  │  └─────────────────────────────┘ │
│                            │  │                                   │
│                            │  │  📜 流程记录                     │
│                            │  │  ● 2026-01-07 审核通过          │
│                            │  │  ● 2026-01-07 提交申请          │
│                            │  │                                   │
└────────────────────────────┘  └───────────────────────────────────┘
```

**抽屉配置：**

- 宽度：`800px`
- 位置：`right`
- 遮罩：`true`
- 点击遮罩关闭：`true`

**抽屉内容（按状态动态显示）：**

| 状态             | 流程步骤    | 操作区域内容                          |
| ---------------- | ----------- | ------------------------------------- |
| `pending_review` | 步骤 2 激活 | [编辑申请] + [取消申请]               |
| `approved`       | 步骤 3 激活 | [下载 supplement.json] + [上传组件包] |
| `rejected`       | 步骤 3 错误 | 显示审核意见                          |
| `completed`      | 步骤 4 完成 | 关联版本链接                          |
| `cancelled`      | 步骤 2 错误 | 已取消提示                            |

#### 上传组件交互（n-upload）

在抽屉中，当状态为 `approved` 时显示上传区域：

```
┌────────────────────────────────────────┐
│  上传组件包                            │
│                                        │
│  ┌────────────────────────────────┐   │
│  │                                │   │
│  │     📦 拖拽文件到此处上传      │   │
│  │        或 点击选择文件         │   │
│  │                                │   │
│  │     支持 .zip 格式，最大 50MB  │   │
│  │                                │   │
│  └────────────────────────────────┘   │
│                                        │
│  上传进度: ████████████░░░░░░ 75%     │
│                                        │
└────────────────────────────────────────┘
```

#### 调用接口

| 操作       | 接口                                             | 方法  |
| ---------- | ------------------------------------------------ | ----- |
| 获取列表   | `/api/development-applications/mine`             | GET   |
| 获取详情   | `/api/development-applications/:id`              | GET   |
| 编辑申请   | `/api/development-applications/:id`              | PATCH |
| 取消申请   | `/api/development-applications/:id/cancel`       | POST  |
| 下载元数据 | `/api/development-applications/:id/export-meta`  | GET   |
| 上传组件包 | `/api/components/upload?applicationId=:id`       | POST  |
| 自助审批   | `/api/development-applications/:id/self-approve` | POST  |

---

### 3.2 创建申请页

#### 页面布局

```
┌─────────────────────────────────────────────────────────────────┐
│  创建研发申请                                                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │  ① 选择类型  ──────  ② 填写信息  ──────  ③ 确认提交       ││
│  │      ●                   ○                   ○              ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                 │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │                                                             ││
│  │   步骤 1: 选择申请类型                                      ││
│  │                                                             ││
│  │   ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     ││
│  │   │   📦 新组件   │  │   🔄 版本迭代 │  │   🔁 替换版本 │     ││
│  │   │              │  │              │  │              │     ││
│  │   │ 创建全新组件 │  │ 为已有组件   │  │ 替换 draft   │     ││
│  │   │              │  │ 添加新版本   │  │ 状态的版本   │     ││
│  │   └──────────────┘  └──────────────┘  └──────────────┘     ││
│  │                                                             ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                 │
│                                           [上一步]  [下一步]    │
└─────────────────────────────────────────────────────────────────┘
```

#### 步骤设计（n-steps）

| 步骤       | 标题     | 内容                                       |
| ---------- | -------- | ------------------------------------------ |
| **步骤 1** | 选择类型 | 三张卡片选择：新组件 / 版本迭代 / 替换版本 |
| **步骤 2** | 填写信息 | 根据类型动态显示表单字段                   |
| **步骤 3** | 确认提交 | 展示填写信息，确认后提交                   |

#### 步骤 2 表单字段（按类型）

##### A. 新组件（`new`）

| 字段     | 组件                | 必填 | 说明                             |
| -------- | ------------------- | ---- | -------------------------------- |
| 组件 ID  | `n-input`           | ✅   | 唯一标识，如 `DataTable`         |
| 组件名称 | `n-input`           | ✅   | 显示名称，如 `数据表格`          |
| 组件分类 | `n-tree-select`     | ✅   | 二级分类树选择（展示到组件层级） |
| 组件描述 | `n-input[textarea]` | ❌   | 简要描述                         |
| 目标版本 | `n-input`           | ✅   | 默认 `1.0.0`                     |
| 更新日志 | `n-input[textarea]` | ❌   | 版本说明                         |

##### B. 版本迭代（`version`）

| 字段     | 组件                | 必填 | 说明                       |
| -------- | ------------------- | ---- | -------------------------- |
| 选择组件 | `n-tree-select`     | ✅   | 树形选择（展示到版本层级） |
| 目标版本 | `n-input`           | ✅   | 需大于当前最高版本         |
| 更新日志 | `n-input[textarea]` | ✅   | 版本变更说明               |

##### C. 替换版本（`replace`）

| 字段     | 组件                | 必填 | 说明                          |
| -------- | ------------------- | ---- | ----------------------------- |
| 选择版本 | `n-tree-select`     | ✅   | 树形选择（仅显示 draft 版本） |
| 更新日志 | `n-input[textarea]` | ✅   | 替换原因说明                  |

#### 分类选择交互（n-tree-select）

根据申请类型，树形选择器展示不同层级：

##### A. 新组件场景（展示到组件层级）

选择器显示：**一级分类 > 二级分类 > 组件**

```
┌──────────────────────────────────────┐
│ 选择组件位置                      ▼  │
├──────────────────────────────────────┤
│ ▶ 📊 图表                            │
│   ▶ 📈 折线图                        │
│     ├── LineChart (折线图组件)       │
│     └── AreaChart (面积图组件)       │
│   ▶ 📊 柱图                          │
│     ├── BarChart (柱状图组件)        │
│     └── [可创建新组件]               │
│   ▶ 🥧 饼图                          │
│ ▶ 📋 表格                            │
│   ▶ 📄 基础表格                      │
│   ▶ 📑 数据表格                      │
│     ├── DataTable (数据表格组件)     │
│     └── [可创建新组件]               │
└──────────────────────────────────────┘
```

**说明：** 选择二级分类后，可在该分类下创建新组件

##### B. 版本迭代场景（展示到版本层级）

选择器显示：**一级分类 > 二级分类 > 组件 > 已有版本**

```
┌──────────────────────────────────────┐
│ 选择要迭代的组件                  ▼  │
├──────────────────────────────────────┤
│ ▶ 📊 图表                            │
│   ▶ 📊 柱图                          │
│     ▶ BarChart (柱状图组件)          │
│       ├── v1.0.0 (published)         │
│       ├── v1.5.0 (published)         │
│       ├── v2.0.0 (draft)             │
│       └── [创建新版本]               │
│ ▶ 📋 表格                            │
│   ▶ 📑 数据表格                      │
│     ▶ DataTable (数据表格组件)       │
│       ├── v1.0.0 (published)         │
│       └── [创建新版本]               │
└──────────────────────────────────────┘
```

**说明：** 展示组件的所有版本，用户选择组件后填写新版本号

##### C. 替换版本场景（展示到版本层级）

选择器显示：**一级分类 > 二级分类 > 组件 > draft 版本**

```
┌──────────────────────────────────────┐
│ 选择要替换的版本                  ▼  │
├──────────────────────────────────────┤
│ ▶ 📊 图表                            │
│   ▶ 📊 柱图                          │
│     ▶ BarChart (柱状图组件)          │
│       ├── v1.0.0 (published) ⚠️不可选│
│       ├── v1.5.0 (published) ⚠️不可选│
│       └── v2.0.0 (draft) ✅ 可选择   │
│ ▶ 📋 表格                            │
│   ▶ 📑 数据表格                      │
│     ▶ DataTable (数据表格组件)       │
│       └── v1.0.0 (published) ⚠️不可选│
└──────────────────────────────────────┘
```

**说明：** 仅显示 `draft` 状态的版本，`published` 版本禁用选择

#### 调用接口

| 操作         | 接口                             | 方法 |
| ------------ | -------------------------------- | ---- |
| 获取分类树   | `/api/component-categories/tree` | GET  |
| 获取组件列表 | `/api/components`                | GET  |
| 获取组件版本 | `/api/components/:id/versions`   | GET  |
| 创建申请     | `/api/development-applications`  | POST |

---

### 3.3 待审核列表页（管理员）

#### 页面布局

```
┌─────────────────────────────────────────────────────────────────┐
│  待审核申请                                                     │
├─────────────────────────────────────────────────────────────────┤
│  筛选区域                                                       │
│  ┌──────────┐ ┌────────────────┐ ┌──────┐                      │
│  │ 类型筛选 │ │ 搜索组件名/ID  │ │ 搜索 │                      │
│  └──────────┘ └────────────────┘ └──────┘                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ 申请单号      │ 类型   │ 组件ID    │ 申请人 │ 时间   │ 操作 ││
│  ├─────────────────────────────────────────────────────────────┤│
│  │ APP-20260107  │ 新组件 │ DataTable │ dev1   │ 01-07  │ 审核 ││
│  │ APP-20260106  │ 迭代   │ BarChart  │ dev2   │ 01-06  │ 审核 ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 审核弹窗（n-modal）

```
┌────────────────────────────────────────┐
│  审核申请 - APP-20260107-0001      ✕   │
├────────────────────────────────────────┤
│                                        │
│  📋 申请摘要                           │
│  ────────────────────────────────────  │
│  组件 ID: DataTable                    │
│  组件名称: 数据表格                    │
│  分类: 表格 > 数据表格                 │
│  目标版本: 1.0.0                       │
│  申请人: developer                     │
│                                        │
│  ────────────────────────────────────  │
│                                        │
│  审核意见 *                            │
│  ┌────────────────────────────────┐   │
│  │                                │   │
│  │                                │   │
│  └────────────────────────────────┘   │
│                                        │
│              [驳回]      [通过]        │
└────────────────────────────────────────┘
```

#### 调用接口

| 操作           | 接口                                           | 方法 |
| -------------- | ---------------------------------------------- | ---- |
| 获取待审核列表 | `/api/development-applications/pending-review` | GET  |
| 审核申请       | `/api/development-applications/:id/review`     | POST |

---

## 4. 状态与交互设计

### 4.1 状态标签样式

| 状态             | 颜色 | Naive UI Type | 图标 |
| ---------------- | ---- | ------------- | ---- |
| `pending_review` | 橙色 | `warning`     | ⏳   |
| `approved`       | 绿色 | `success`     | ✅   |
| `rejected`       | 红色 | `error`       | ❌   |
| `completed`      | 深绿 | `success`     | 🎉   |
| `cancelled`      | 灰色 | `default`     | 🚫   |

### 4.2 申请类型标签

| 类型      | 显示文本 | Naive UI Type |
| --------- | -------- | ------------- |
| `new`     | 新组件   | `success`     |
| `version` | 版本迭代 | `info`        |
| `replace` | 替换版本 | `warning`     |

### 4.3 操作按钮可见性矩阵

| 操作       | pending_review | approved | rejected | completed | cancelled |
| ---------- | -------------- | -------- | -------- | --------- | --------- |
| 编辑申请   | ✅             | ❌       | ❌       | ❌        | ❌        |
| 取消申请   | ✅             | ❌       | ❌       | ❌        | ❌        |
| 下载元数据 | ❌             | ✅       | ❌       | ✅        | ❌        |
| 上传组件   | ❌             | ✅       | ❌       | ❌        | ❌        |
| 审核操作   | ✅             | ❌       | ❌       | ❌        | ❌        |

### 4.4 流程步骤映射

```typescript
// n-steps 当前步骤计算
function getCurrentStep(status: string): number {
  const stepMap = {
    pending_review: 1, // 等待审核
    approved: 2, // 审核通过，待上传
    rejected: 2, // 审核不通过（错误状态）
    completed: 3, // 已完成
    cancelled: 1 // 已取消（错误状态）
  }
  return stepMap[status] ?? 0
}

// n-steps 状态计算
function getStepStatus(status: string): 'process' | 'finish' | 'error' {
  if (status === 'rejected' || status === 'cancelled') {
    return 'error'
  }
  if (status === 'completed') {
    return 'finish'
  }
  return 'process'
}
```

---

## 5. API 接口与 DTO

### 5.1 接口总览

| 接口                                             | 方法  | 权限           | 说明         |
| ------------------------------------------------ | ----- | -------------- | ------------ |
| `/api/development-applications`                  | POST  | `create`       | 创建申请     |
| `/api/development-applications/mine`             | GET   | `read`         | 我的申请列表 |
| `/api/development-applications/pending-review`   | GET   | `review`       | 待审核列表   |
| `/api/development-applications/:id`              | GET   | `read`         | 申请详情     |
| `/api/development-applications/:id`              | PATCH | `update`       | 编辑申请     |
| `/api/development-applications/:id/export-meta`  | GET   | `read`         | 导出元数据   |
| `/api/development-applications/:id/review`       | POST  | `review`       | 审核申请     |
| `/api/development-applications/:id/self-approve` | POST  | `self-approve` | 自助审批     |
| `/api/development-applications/:id/cancel`       | POST  | `update`       | 取消申请     |
| `/api/components/upload`                         | POST  | `create`       | 上传组件包   |

---

### 5.2 创建申请

**POST** `/api/development-applications`

#### Request DTO

```typescript
// 新组件
interface CreateNewApplicationDto {
  applicationType: 'new'
  componentId: string // 组件唯一标识
  name: string // 组件名称
  description?: string // 组件描述
  classificationLevel1: string // 一级分类标识
  classificationLevel2: string // 二级分类标识
  targetVersion: string // 目标版本，默认 1.0.0
  changelog?: string // 更新日志
}

// 版本迭代
interface CreateVersionApplicationDto {
  applicationType: 'version'
  componentId: string // 已有组件 ID
  targetVersion: string // 新版本号
  changelog: string // 更新日志
}

// 替换版本
interface CreateReplaceApplicationDto {
  applicationType: 'replace'
  componentId: string // 已有组件 ID
  existingVersionId: number // 要替换的版本 ID
  changelog: string // 替换原因
}
```

#### Response DTO

```typescript
interface CreateApplicationResponseDto {
  message: string
  data: {
    id: number
    applicationNo: string
    developmentStatus: 'pending_review'
    createdAt: string
  }
}
```

---

### 5.3 查询我的申请列表

**GET** `/api/development-applications/mine`

#### Query Parameters

| 参数              | 类型   | 说明                         |
| ----------------- | ------ | ---------------------------- |
| `page`            | number | 页码，默认 1                 |
| `pageSize`        | number | 每页数量，默认 10            |
| `status`          | string | 状态筛选（可多选，逗号分隔） |
| `applicationType` | string | 类型筛选                     |
| `keyword`         | string | 搜索关键词                   |

#### Response DTO

```typescript
interface ApplicationListResponseDto {
  message: string
  data: {
    items: ApplicationItemDto[]
    total: number
    page: number
    pageSize: number
  }
}

interface ApplicationItemDto {
  id: number
  applicationNo: string
  applicationType: 'new' | 'version' | 'replace'
  applicationTypeLabel: string
  componentId: string
  name: string | null
  targetVersion: string
  developmentStatus: string
  developmentStatusLabel: string
  createdAt: string
  updatedAt: string
}
```

---

### 5.4 查询申请详情

**GET** `/api/development-applications/:id`

#### Response DTO

```typescript
interface ApplicationDetailResponseDto {
  message: string
  data: {
    id: number
    applicationNo: string
    applicationType: 'new' | 'version' | 'replace'
    applicationTypeLabel: string
    componentId: string
    name: string | null
    description: string | null
    classificationLevel1: string | null
    classificationLevel2: string | null
    classificationLevel1Name: string | null
    classificationLevel2Name: string | null
    targetVersion: string
    changelog: string | null
    developmentStatus: string
    developmentStatusLabel: string
    uploadInfo: {
      fileName: string
      fileSize: number
      uploadTime: string
      ossPath: string
    } | null
    reviewInfo: {
      reviewerId: number
      reviewerName: string
      reviewTime: string
      reviewAction: 'approve' | 'reject'
      reviewComment: string
    } | null
    componentVersionId: number | null
    existingVersionId: number | null
    applicant: {
      id: number
      username: string
    }
    reviewer: {
      id: number
      username: string
    } | null
    reviewedAt: string | null
    completedAt: string | null
    createdAt: string
    updatedAt: string
  }
}
```

---

### 5.5 编辑申请

**PATCH** `/api/development-applications/:id`

#### Request DTO

```typescript
interface UpdateApplicationDto {
  name?: string // 仅新组件可改
  description?: string
  classificationLevel1?: string // 仅新组件可改
  classificationLevel2?: string // 仅新组件可改
  changelog?: string
}
```

#### Response DTO

```typescript
interface UpdateApplicationResponseDto {
  message: string
  data: ApplicationDetailResponseDto['data']
}
```

---

### 5.6 审核申请

**POST** `/api/development-applications/:id/review`

#### Request DTO

```typescript
interface ReviewApplicationDto {
  reviewAction: 'approve' | 'reject'
  reviewComment: string
}
```

#### Response DTO

```typescript
interface ReviewApplicationResponseDto {
  message: string
  data: {
    developmentStatus: 'approved' | 'rejected'
    reviewInfo: {
      reviewerId: number
      reviewerName: string
      reviewTime: string
      reviewAction: 'approve' | 'reject'
      reviewComment: string
    }
  }
}
```

---

### 5.7 导出元数据

**GET** `/api/development-applications/:id/export-meta`

#### Response DTO（JSON 文件下载）

```typescript
interface ExportMetaResponseDto {
  id: string // 组件 ID
  name: string // 组件名称
  version: string // 版本号
  classification: {
    level1: string // 一级分类标识
    level2: string // 二级分类标识
    displayName: {
      level1: string // 一级分类名称
      level2: string // 二级分类名称
    }
  }
  _metadata: {
    applicationId: number
    applicationNo: string
    exportTime: string
    isReplacement?: boolean // 替换版本场景
    originalVersionId?: number // 替换版本场景
  }
}
```

---

### 5.8 取消申请

**POST** `/api/development-applications/:id/cancel`

#### Response DTO

```typescript
interface CancelApplicationResponseDto {
  message: string
  data: {
    developmentStatus: 'cancelled'
    cancelledAt: string
  }
}
```

---

### 5.9 上传组件包

**POST** `/api/components/upload?applicationId=:id`

#### Request

```
Content-Type: multipart/form-data

file: <组件包 .zip 文件>
```

#### Response DTO（成功）

```typescript
interface UploadSuccessResponseDto {
  message: string
  data: {
    componentId: string
    versionId: number
    version: string
    ossPath: string
    developmentStatus: 'completed'
  }
}
```

#### Response DTO（失败）

```typescript
interface UploadErrorResponseDto {
  code: number
  message: string
  error: string
  details?: {
    missingFiles?: string[]
    validationErrors?: string[]
  }
}
```

---

### 5.10 待审核列表

**GET** `/api/development-applications/pending-review`

#### Query Parameters

| 参数              | 类型   | 说明              |
| ----------------- | ------ | ----------------- |
| `page`            | number | 页码，默认 1      |
| `pageSize`        | number | 每页数量，默认 10 |
| `applicationType` | string | 类型筛选          |
| `keyword`         | string | 搜索关键词        |

#### Response DTO

```typescript
interface PendingReviewListResponseDto {
  message: string
  data: {
    items: PendingReviewItemDto[]
    total: number
    page: number
    pageSize: number
  }
}

interface PendingReviewItemDto {
  id: number
  applicationNo: string
  applicationType: 'new' | 'version' | 'replace'
  applicationTypeLabel: string
  componentId: string
  name: string | null
  targetVersion: string
  applicant: {
    id: number
    username: string
  }
  createdAt: string
}
```

---

### 5.11 自助审批

**POST** `/api/development-applications/:id/self-approve`

> 管理员审批自己的申请，用于快速上传场景

#### Request DTO

```typescript
interface SelfApproveDto {
  reviewAction: 'approve' // 仅支持通过
  reviewComment?: string // 可选
}
```

#### Response DTO

```typescript
interface SelfApproveResponseDto {
  message: string
  data: {
    developmentStatus: 'approved'
    reviewInfo: {
      reviewerId: number
      reviewerName: string
      reviewTime: string
      reviewAction: 'approve'
      reviewComment: string
    }
  }
}
```
