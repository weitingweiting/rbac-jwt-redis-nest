meta {
  name: 编辑者路由 - 权限验证
  type: http
  seq: 3
}

get {
  url: {{usersBaseUrl}}/editor
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
}

tests {
  test("编辑者路由访问成功", function() {
    expect(res.status).to.equal(200);
  });
}