meta {
  name: 无Token访问 - 权限拒绝
  type: http
  seq: 20
}

get {
  url: {{usersBaseUrl}}
  body: none
  auth: none
}

assert {
  res.status: eq 401
}

tests {
  test("无Token访问被拒绝", function() {
    expect(res.status).to.equal(401);
    expect(res.body.data.message).to.include('未授权');
  });
}