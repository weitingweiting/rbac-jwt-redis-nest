meta {
  name: 高级权限路由 - 多权限验证
  type: http
  seq: 4
}

get {
  url: {{usersBaseUrl}}/advanced
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
}

tests {
  test("高级权限路由访问成功", function() {
    expect(res.status).to.equal(200);
  });
}