meta {
  name: 获取个人资料 - 权限验证
  type: http
  seq: 2
}

get {
  url: {{usersBaseUrl}}/profile
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
}

tests {
  test("个人资料访问成功", function() {
    expect(res.status).to.equal(200);
  });
}