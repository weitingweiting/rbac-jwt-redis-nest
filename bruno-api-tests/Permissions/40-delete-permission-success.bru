meta {
  name: 删除权限 - 成功
  type: http
  seq: 40
}

delete {
  url: {{permissionsBaseUrl}}/{{createdPermissionId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
}

tests {
  test("删除权限成功", function() {
    expect(res.status).to.equal(200);
  });
}
