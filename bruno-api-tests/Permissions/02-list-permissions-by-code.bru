meta {
  name: 获取权限列表 - 按code查询
  type: http
  seq: 2
}

get {
  url: {{permissionsBaseUrl}}?code=project&page=1&limit=10
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
}

tests {
  test("按code查询权限成功", function() {
    expect(res.status).to.equal(200);
    expect(res.body.data).to.have.property('items');
    expect(res.body.data.items).to.be.an('array');
  });
}
