meta {
  name: 删除角色 - 成功
  type: http
  seq: 50
}

delete {
  url: {{rolesBaseUrl}}/5
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
}

tests {
  test("删除角色成功", function() {
    expect(res.status).to.equal(200);
  });
}
