meta {
  name: 更新角色 - 成功
  type: http
  seq: 30
}

put {
  url: {{rolesBaseUrl}}/4
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "description": "更新后的描述,admin的测试角色"
  }
}

assert {
  res.status: eq 200
}

tests {
  test("更新角色成功", function() {
    expect(res.status).to.equal(200);
    expect(res.body.data).to.have.property('id');
    expect(res.body.data.description).to.equal("更新后的描述");
  });
}
