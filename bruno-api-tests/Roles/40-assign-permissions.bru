meta {
  name: 为角色分配权限
  type: http
  seq: 40
}

put {
  url: {{rolesBaseUrl}}/7/permissions
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "permissionIds": [
      26,
      27,
      28,
      29,
      34,
      35,
      36,
      37,
      38,
      39,
      40,
      41
    ]
  }
}

assert {
  res.status: eq 200
}

tests {
  test("分配权限成功", function() {
    console.log( res )
    expect(res.status).to.equal(200);
    expect(res.body.data.data).to.have.property('permissions');
    expect(res.body.data.data.permissions).to.be.an('array');
  });
}
