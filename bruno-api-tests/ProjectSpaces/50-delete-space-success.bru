meta {
  name: 删除项目空间 - 成功
  type: http
  seq: 50
}

delete {
  url: {{projectSpacesBaseUrl}}/{{createdSpaceId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
}

tests {
  test("删除项目空间成功", function() {
    expect(res.status).to.equal(200);
  });
}
