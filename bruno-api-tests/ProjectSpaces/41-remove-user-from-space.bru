meta {
  name: 从项目空间移除用户 - 成功
  type: http
  seq: 41
}

delete {
  url: {{projectSpacesBaseUrl}}/10/users/100
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
}

tests {
  test("从项目空间移除用户成功", function() {
    expect(res.status).to.equal(200);
    expect(res.body.data).to.have.property('users');
    expect(res.body.data.users).to.be.an('array');
  });
}
