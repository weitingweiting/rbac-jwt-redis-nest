meta {
  name: 获取单个项目空间 - 不存在
  type: http
  seq: 11
}

get {
  url: {{projectSpacesBaseUrl}}/99999
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 404
}

tests {
  test("项目空间不存在返回404", function() {
    expect(res.status).to.equal(404);
    expect(res.body).to.have.property('message');
  });
}
