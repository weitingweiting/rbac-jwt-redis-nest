meta {
  name: 添加用户到项目空间 - 成功
  type: http
  seq: 40
}

put {
  url: {{projectSpacesBaseUrl}}/12/users
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "userIds": [5]
  }
}

assert {
  res.status: eq 200
}

tests {
  test("添加用户到项目空间成功", function() {
    expect(res.status).to.equal(200);
    expect(res.body.data).to.have.property('users');
    expect(res.body.data.users).to.be.an('array');
  });
}
