meta {
  name: 为用户分配角色 - 成功
  type: http
  seq: 42
}

put {
  url: {{usersBaseUrl}}/6/roles
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "roleIds": [1]
  }
}

assert {
  res.status: eq 200
}

tests {
  test("更新用户成功", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.have.property('message', '分配角色成功');
  });
}
