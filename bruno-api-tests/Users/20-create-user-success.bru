meta {
  name: 创建用户 - 成功
  type: http
  seq: 20
}

post {
  url: {{usersBaseUrl}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "username": "weiting1",
    "password": "Weiting1"
  }
}

assert {
  res.status: eq 201
}

tests {
  test("创建用户成功", function() {
    expect(res.status).to.equal(201);
    expect(res.body).to.have.property('id');
    expect(res.body).to.have.property('username');
  });
}
