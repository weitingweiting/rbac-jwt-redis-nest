meta {
  name: 管理员重置用户密码
  type: http
  seq: 60
}

put {
  url: {{usersBaseUrl}}/5/reset-password
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "newPassword": "Weiting1"
  }
}

tests {
  test("Status code is 200", function() {
    expect(res.status).to.equal(200);
  });
  
  test("Response should have success message", function() {
    const body = res.body;
    expect(body.data.message).to.include("密码已重置");
  });
}
