meta {
  name: 更新用户 - 成功
  type: http
  seq: 30
}

put {
  url: {{usersBaseUrl}}/5
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "username": "weiting01"
  }
}

assert {
  res.status: eq 200
}

tests {
  test("更新用户成功", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.have.property('username', 'updatedUser');
  });
}
