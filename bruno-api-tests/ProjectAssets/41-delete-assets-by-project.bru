meta {
  name: 批量删除项目的所有资源
  type: http
  seq: 41
}

delete {
  url: {{projectAssetsBaseUrl}}/project/{{projectId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
}

tests {
  test("批量删除项目资源成功", function() {
    expect(res.status).to.equal(200);
  });
}
