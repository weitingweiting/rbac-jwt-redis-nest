meta {
  name: 11-代理请求-无效URL失败
  type: http
  seq: 11
}

post {
  url: {{base_url}}/proxy
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "targetUrl": "not-a-valid-url",
    "targetMethod": "GET"
  }
}

tests {
  test("状态码应为 400", function() {
    expect(res.status).to.equal(400)
  })
  
  test("应返回URL格式错误", function() {
    expect(res.body.message).to.exist
  })
}
