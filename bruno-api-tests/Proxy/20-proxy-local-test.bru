meta {
  name: 20-代理到本地测试接口-成功
  type: http
  seq: 20
}

post {
  url: {{base_url}}/proxy
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "targetUrl": "{{base_url}}/proxy-test/success",
    "targetMethod": "GET"
  }
}

tests {
  test("状态码应为 200", function() {
    expect(res.status).to.equal(200)
  })
  
  test("应返回测试数据", function() {
    expect(res.body).to.have.property('code')
    expect(res.body.code).to.equal(0)
    expect(res.body.data).to.have.property('name')
  })
}
